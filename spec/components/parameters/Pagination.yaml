Offset:
  name: offset
  in: query
  description: Pagination offset
  required: true
  schema:
    type: integer
Limit:
  name: limit
  in: query
  description: Pagination limit
  required: true
  schema:
    type: integer